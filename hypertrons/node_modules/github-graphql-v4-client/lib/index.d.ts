import Logger = require('bunyan');
interface ClientOption {
    tokens: string[];
    logger?: Logger;
    maxConcurrentReqNumber?: number;
    filterStatusCode?: number[];
    maxRetryTimes?: number;
}
export declare class GitHubClient {
    private tokens;
    private maxConcurrentReqNumber;
    private concurrentReqNumber;
    private logger;
    private getConnectionRetryInterval;
    private filterStatusCode;
    private requestCostPrediction;
    private maxRetryTimes;
    private inited;
    constructor(options: ClientOption);
    init(): Promise<void>;
    query<TR, T>(q: string, p: T): Promise<TR>;
    private resetToken;
    private initToken;
    private getToken;
    private hasSufficientRemaing;
    private internalQuery;
}
export {};
